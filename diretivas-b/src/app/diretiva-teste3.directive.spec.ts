import { DiretivaTeste3Directive } from './diretiva-teste3.directive';

describe('DiretivaTeste3Directive', () => {
  it('should create an instance', () => {
    const directive = new DiretivaTeste3Directive();
    expect(directive).toBeTruthy();
  });
});
