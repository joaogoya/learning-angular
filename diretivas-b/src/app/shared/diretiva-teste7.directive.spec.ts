import { DiretivaTeste7Directive } from './diretiva-teste7.directive';

describe('DiretivaTeste7Directive', () => {
  it('should create an instance', () => {
    const directive = new DiretivaTeste7Directive();
    expect(directive).toBeTruthy();
  });
});
