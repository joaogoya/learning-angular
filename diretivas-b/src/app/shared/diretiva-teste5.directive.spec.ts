import { DiretivaTeste5Directive } from './diretiva-teste5.directive';

describe('DiretivaTeste5Directive', () => {
  it('should create an instance', () => {
    const directive = new DiretivaTeste5Directive();
    expect(directive).toBeTruthy();
  });
});
