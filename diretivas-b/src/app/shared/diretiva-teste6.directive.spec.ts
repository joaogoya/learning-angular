import { DiretivaTeste6Directive } from './diretiva-teste6.directive';

describe('DiretivaTeste6Directive', () => {
  it('should create an instance', () => {
    const directive = new DiretivaTeste6Directive();
    expect(directive).toBeTruthy();
  });
});
