import { DiretivaTesteDirective } from './diretiva-teste.directive';

describe('DiretivaTesteDirective', () => {
  it('should create an instance', () => {
    const directive = new DiretivaTesteDirective();
    expect(directive).toBeTruthy();
  });
});
