import { DiretivaTeste8Directive } from './diretiva-teste8.directive';

describe('DiretivaTeste8Directive', () => {
  it('should create an instance', () => {
    const directive = new DiretivaTeste8Directive();
    expect(directive).toBeTruthy();
  });
});
